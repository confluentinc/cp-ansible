---
# External Prometheus mTLS: bundle absent, C3 reads the external Prometheus over mTLS, alerts off, nodes pushing.
- import_playbook: ../external_prometheus_verify.yml

# Negative: validate_external_prometheus rejects bad external configs.
