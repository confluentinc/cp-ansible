---
### Checks services run as the deploy user via systemd --user, nothing landed
### under /opt, and SASL_PLAIN (+ SSL) is actually configured on each component.

- name: Verify - rootless lifecycle on all CP components
  hosts: kafka_controller:kafka_broker:schema_registry:kafka_connect:kafka_rest:ksql:control_center
  gather_facts: true
  vars:
    rootless_unit_map:
      kafka_controller: cp-kafka_controller.service
      kafka_broker: cp-kafka_broker.service
      schema_registry: cp-schema_registry.service
      kafka_connect: cp-kafka_connect.service
      kafka_rest: cp-kafka_rest.service
      ksql: cp-ksql.service
      control_center: cp-control_center.service
  tasks:
    - name: Determine this host's rootless unit name
      set_fact:
        rootless_unit: "{{ rootless_unit_map[group_names | intersect(rootless_unit_map.keys()) | first] }}"

    - name: Check the systemd --user unit is active
      command: "systemctl --user is-active {{ rootless_unit }}"
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid }}"
      register: unit_active
      changed_when: false
      failed_when: unit_active.stdout != "active"

    - name: Check the process is owned by the deploy user, not root
      shell: "ps -eo user,cmd | grep -E 'kafka.Kafka|SchemaRegistryMain|ConnectDistributed|KafkaRestMain|KsqlServerMain|ControlCenter' | grep -v grep | awk '{print $1}' | sort -u"
      register: process_owners
      changed_when: false

    - name: Assert no process runs as root
      assert:
        that:
          - "'root' not in process_owners.stdout_lines"
          - process_owners.stdout_lines | length > 0
        fail_msg: "Expected all component processes to run as {{ deployment_user }}, found: {{ process_owners.stdout_lines }}"
        quiet: true

    - name: Check nothing was written under /opt
      find:
        paths: /opt
        recurse: true
      register: opt_contents

    - name: Assert nothing besides the pre-baked custom_java_path JDK exists under /opt
      # Dockerfile-debian10-archive.j2 tars a JDK into custom_java_path (/opt/jdk17) at image
      # build time, before Ansible ever runs - that's not the rootless install writing to /opt.
      vars:
        unexpected_opt_files: "{{ opt_contents.files | rejectattr('path', 'match', '^' + custom_java_path + '/') | map(attribute='path') | list }}"
      assert:
        that:
          - unexpected_opt_files | length == 0
        fail_msg: "Rootless install wrote to /opt outside custom_java_path: {{ unexpected_opt_files }}"
        quiet: true

- name: Verify - SASL_PLAIN configured on kafka_controller
  hosts: kafka_controller
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/controller/server.properties"
        property: listener.name.controller.sasl.enabled.mechanisms
        expected_value: PLAIN

- name: Verify - SASL_PLAIN configured on kafka_broker
  hosts: kafka_broker
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/kafka/server.properties"
        property: listener.name.internal.sasl.enabled.mechanisms
        expected_value: PLAIN

- name: Verify - SASL_PLAIN configured on schema_registry
  hosts: schema_registry
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/schema-registry/schema-registry.properties"
        property: kafkastore.security.protocol
        expected_value: SASL_SSL

- name: Verify - SASL_PLAIN configured on kafka_rest
  hosts: kafka_rest
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/kafka-rest/kafka-rest.properties"
        property: client.security.protocol
        expected_value: SASL_SSL

- name: Verify - SASL_PLAIN configured on kafka_connect
  hosts: kafka_connect
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
        property: security.protocol
        expected_value: SASL_SSL

- name: Verify - SASL_PLAIN configured on ksql
  hosts: ksql
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/ksqldb/ksql-server.properties"
        property: security.protocol
        expected_value: SASL_SSL

- name: Verify - kafka_connect plugin + connector deployed (rootless)
  hosts: kafka_connect
  gather_facts: false
  tasks:
    - name: Get Connectors on connect cluster1
      uri:
        url: "https://localhost:8083/connectors"
        validate_certs: false
      register: connectors

    - name: Assert Connector Created
      assert:
        that:
          - connectors.json[0] == "sample-connector-1"
        fail_msg: "Connector not created"

    - name: Wait for Connector tasks to return Running
      uri:
        url: "https://localhost:8083/connectors/sample-connector-1/status"
        validate_certs: false
      register: connector_status_response
      until: connector_status_response.json.tasks[0].state == 'RUNNING'
      retries: 10
      delay: 5

- name: Verify - SASL_PLAIN configured on control_center
  hosts: control_center
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ deployment_path }}/confluent/etc/confluent-control-center/control-center-production.properties"
        property: confluent.controlcenter.streams.security.protocol
        expected_value: SASL_SSL
