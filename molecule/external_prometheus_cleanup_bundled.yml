---
# External Prometheus brownfield test helper: run the customer-facing teardown playbook so the migration scenarios
# exercise the real deliverable (single source of truth) rather than a test-only copy. The customer
# runs the same playbook manually after verifying C3 reads the external Prometheus.
# See playbooks/external_prometheus_teardown_bundled_monitoring.yml.
- import_playbook: confluent.platform.external_prometheus_teardown_bundled_monitoring
