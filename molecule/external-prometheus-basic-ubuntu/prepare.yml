---
- name: Configure IPv6 Preference
  import_playbook: ../configure_ipv6.yml

# External Prometheus Basic auth: distribute only the mock Prometheus CA (so C3 and the nodes trust the server
# cert). No client cert is needed - the mock uses Basic auth over TLS, not mutual TLS.
- import_playbook: ../external_prometheus_distribute_ca.yml
