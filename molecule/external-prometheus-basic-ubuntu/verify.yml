---
# External Prometheus Basic auth: bundle absent, C3 reads the external Prometheus over TLS+Basic auth, alerts off,
# nodes pushing.
- import_playbook: ../external_prometheus_verify.yml

# Negative: validate_external_prometheus rejects bad external configs (TLS off, both auth modes, no auth mode).
- import_playbook: ../external_prometheus_validate_negative.yml
