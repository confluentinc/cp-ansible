---
### SR Automation Workflow Testing Converge - LDAP/RBAC + mTLS
### Imports base converge and adds LDAP + mTLS specific testing

- import_playbook: ../base_templates/sr_automation_converge_base.yml
  vars:
    test_automation_tags: "schema_exporter"  # Test exporter setup with LDAP + mTLS authentication
