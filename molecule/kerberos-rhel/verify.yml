---
### Validates that Kerberos is enabled across all components.
### Validates that SASL SSL Plaintext is enabled across all components.
### Validates that Connector is running.
### Validates the rootless systemd --user lifecycle: nothing ran as root, nothing landed
### under /opt beyond the pre-baked JDK, keytabs/krb5.conf landed under
### rootless_deployment_path (not /etc), and the generated unit/env files point there too.

- name: Verify - kafka_controller
  hosts: kafka_controller
  gather_facts: false
  tasks:
    - import_role:
        name: variables
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/controller/server.properties"
        property: controller.quorum.voters
        expected_value: "{{ kafka_controller_quorum_voters }}"
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/controller/server.properties"
        property: sasl.enabled.mechanisms
        expected_value: GSSAPI
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/controller/server.properties"
        property: sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - kafka_broker
  hosts: kafka_broker
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka/server.properties"
        property: sasl.enabled.mechanisms
        expected_value: GSSAPI

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka/server.properties"
        property: sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - schema_registry
  hosts: schema_registry
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/schema-registry/schema-registry.properties"
        property: kafkastore.security.protocol
        expected_value: SASL_PLAINTEXT

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/schema-registry/schema-registry.properties"
        property: kafkastore.sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - kafka_connect
  hosts: kafka_connect
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
        property: security.protocol
        expected_value: SASL_PLAINTEXT

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
        property: sasl.kerberos.service.name
        expected_value: kafka-a

    - name: Get Connectors on connect cluster1
      uri:
        url: "http://kafka-connect1:8083/connectors"
        status_code: 200
        validate_certs: false
      register: connectors

    - name: Assert Connector Created
      assert:
        that:
          - connectors.json[0] == "sample-connector-1"
        fail_msg: "Connector not created"
        quiet: true

    - import_role:
        name: variables

    - name: Test data
      set_fact:
        kafka_connect_url: "{{kafka_connect_http_protocol}}://{{ hostvars[inventory_hostname]|confluent.platform.resolve_hostname }}:{{kafka_connect_rest_port}}/connectors"
        reset_all_connectors: []

        add_new_connector:
          - name: test-connector-1
            config:
              connector.class: "FileStreamSourceConnector"
              tasks.max: "1"
              file: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
              topic: "test_topic"

        update_existing_connector:
          - name: test-connector-1
            config:
              connector.class: "FileStreamSourceConnector"
              tasks.max: "5"
              file: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
              topic: "test_topic"

        add_bad_request_connector:
          - name: test-connector-1
            config:
              connector.class: "FileStreamSourceConnector"
              tasks.max: "5"
              file: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
              topic: "test_topic"
          - name: test-connector-2
            config:
              connector.class: "org.apache.kafka.connect.mirror.MirrorHeartbeatConnector"
              tasks.max: "1"
              file: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
              topics: "foo"

        add_non_existent_connector:
          - name: test-connector-1
            config:
              connector.class: "FileStreamSourceConnector"
              tasks.max: "5"
              file: "{{ rootless_deployment_path }}/confluent/etc/kafka/connect-distributed.properties"
              topic: "test_topic"
          - name: test-connector-2
            config:
              connector.class: "io.confluent.connect.jdbc.JdbcSinkConnector"
              tasks.max: "1"
              topics: "foo"

        delete_connector:
          - name: test-connector-2
            config:
              connector.class: "FileStreamSourceConnector"
              tasks.max: "1"
              topic: "bar"

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ reset_all_connectors }}"
        expected_message: ""

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ add_new_connector }}"
        expected_message: "Connectors added or updated: test-connector-1: new connector added."

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ update_existing_connector }}"
        expected_message: "Connectors added or updated: test-connector-1: connector configuration updated."

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ add_bad_request_connector }}"
        expected_message: "Connectors added or updated: test-connector-1: no configuration change, test-connector-2: ERROR error while adding new connector configuration (HTTP Error 400: Bad Request)."

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ add_non_existent_connector }}"
        expected_message: "Connectors added or updated: test-connector-1: no configuration change, test-connector-2: ERROR error while adding new connector configuration (HTTP Error 500: Internal Server Error)."

    - import_role:
        name: confluent.test
        tasks_from: check_connector.yml
      vars:
        connect_url_input: "{{ kafka_connect_url }}"
        active_connectors_input: "{{ delete_connector }}"
        expected_message: "Connectors removed: test-connector-1. Connectors added or updated: test-connector-2: new connector added."

- name: Verify - kafka_rest
  hosts: kafka_rest
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka-rest/kafka-rest.properties"
        property: client.security.protocol
        expected_value: SASL_PLAINTEXT

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/kafka-rest/kafka-rest.properties"
        property: client.sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - ksql
  hosts: ksql
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/ksqldb/ksql-server.properties"
        property: security.protocol
        expected_value: SASL_PLAINTEXT

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/ksqldb/ksql-server.properties"
        property: sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - control_center
  hosts: control_center
  gather_facts: false
  tasks:
    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/confluent-control-center/control-center-production.properties"
        property: confluent.controlcenter.streams.security.protocol
        expected_value: SASL_PLAINTEXT

    - import_role:
        name: confluent.test
        tasks_from: check_property.yml
      vars:
        file_path: "{{ rootless_deployment_path }}/confluent/etc/confluent-control-center/control-center-production.properties"
        property: confluent.controlcenter.streams.sasl.kerberos.service.name
        expected_value: kafka-a

- name: Verify - rootless lifecycle on all CP components
  hosts: kafka_controller:kafka_broker:schema_registry:kafka_connect:kafka_rest:ksql:control_center
  gather_facts: true
  vars:
    rootless_unit_map:
      kafka_controller: cp-kafka_controller.service
      kafka_broker: cp-kafka_broker.service
      schema_registry: cp-schema_registry.service
      kafka_connect: cp-kafka_connect.service
      kafka_rest: cp-kafka_rest.service
      ksql: cp-ksql.service
      control_center: cp-control_center.service
  tasks:
    - name: Determine this host's rootless unit name and component name
      set_fact:
        rootless_unit: "{{ rootless_unit_map[group_names | intersect(rootless_unit_map.keys()) | first] }}"
        rootless_component: "{{ group_names | intersect(rootless_unit_map.keys()) | first }}"

    - name: Check the systemd --user unit is active
      command: "systemctl --user is-active {{ rootless_unit }}"
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid }}"
      register: unit_active
      changed_when: false
      failed_when: unit_active.stdout != "active"

    - name: Check the process is owned by the deploy user, not root
      shell: "ps -eo user,cmd | grep -E 'kafka.Kafka|SchemaRegistryMain|ConnectDistributed|KafkaRestMain|KsqlServerMain|ControlCenter' | grep -v grep | awk '{print $1}' | sort -u"
      register: process_owners
      changed_when: false

    - name: Assert no process runs as root
      assert:
        that:
          - "'root' not in process_owners.stdout_lines"
          - process_owners.stdout_lines | length > 0
        fail_msg: "Expected all component processes to run as {{ deployment_user }}, found: {{ process_owners.stdout_lines }}"
        quiet: true

    - name: Assert this component's rendered keytab_path variable points under rootless_deployment_path
      vars:
        rendered_keytab_path: "{{ lookup('vars', rootless_component + '_keytab_path') }}"
      assert:
        that:
          - rootless_deployment_path in rendered_keytab_path
        fail_msg: "{{ rootless_component }}_keytab_path doesn't point under rootless_deployment_path: {{ rendered_keytab_path }}"
        quiet: true

    - name: Check the keytab actually landed at that path on this host, not root-owned
      stat:
        path: "{{ lookup('vars', rootless_component + '_keytab_path') }}"
      register: remote_keytab

    - name: Assert the keytab file exists and is owned by the deploy user
      assert:
        that:
          - remote_keytab.stat.exists
          - remote_keytab.stat.pw_name == deployment_user
        fail_msg: "Keytab missing or root-owned at {{ lookup('vars', rootless_component + '_keytab_path') }}: {{ remote_keytab.stat }}"
        quiet: true

    - name: Assert krb5.conf landed under rootless_deployment_path, not /etc
      stat:
        path: "{{ kerberos_client_config_file_dest }}"
      register: krb5_conf
      failed_when: not krb5_conf.stat.exists

    - name: Find everything under rootless_deployment_path
      find:
        paths: "{{ rootless_deployment_path }}"
        recurse: true
        file_type: any
      register: rootless_deployment_path_contents

    - name: Assert everything under rootless_deployment_path is owned by the deploy user, not root
      # A privileged test container can docker-exec as root regardless of what the deploy
      # itself did - this, not the container's own privilege level, is what actually proves
      # nothing in the deploy path used root: a root-owned file here means some task silently
      # became root.
      vars:
        non_deploy_user_files: "{{ rootless_deployment_path_contents.files | rejectattr('pw_name', 'equalto', deployment_user) | map(attribute='path') | list }}"
      assert:
        that:
          - non_deploy_user_files | length == 0
        fail_msg: "Found files under rootless_deployment_path not owned by {{ deployment_user }}: {{ non_deploy_user_files }}"
        quiet: true

    - name: Read the generated systemd --user unit file
      slurp:
        src: "{{ ansible_env.HOME }}/.config/systemd/user/{{ rootless_unit }}"
      register: unit_file_slurp

    - name: Assert the unit's ExecStart/EnvironmentFile point under rootless_deployment_path, not a hardcoded root path
      vars:
        unit_content: "{{ unit_file_slurp.content | b64decode }}"
      assert:
        that:
          - rootless_deployment_path in unit_content
          - ("EnvironmentFile=" ~ rootless_deployment_path) in unit_content
        fail_msg: "cp-{{ rootless_component }} unit file doesn't reference rootless_deployment_path as expected:\n{{ unit_content }}"
        quiet: true

    - name: Check nothing was written under /opt
      find:
        paths: /opt
        recurse: true
      register: opt_contents

    - name: Assert nothing besides the pre-baked custom_java_path JDK exists under /opt
      # Dockerfile-rhel-java8.j2 tars a JDK into custom_java_path (/opt/jdk17) at image build
      # time, before Ansible ever runs - that's not the rootless install writing to /opt.
      vars:
        unexpected_opt_files: "{{ opt_contents.files | rejectattr('path', 'match', '^' + custom_java_path + '/') | map(attribute='path') | list }}"
      assert:
        that:
          - unexpected_opt_files | length == 0
        fail_msg: "Rootless install wrote to /opt outside custom_java_path: {{ unexpected_opt_files }}"
        quiet: true
