FROM {{ item.image }}

RUN mkdir -p /usr/share/man/man1 && \
    apt-get update -y && \
    apt-get install -y openjdk-21-jdk wget gnupg vim jq && \
    apt-get install -y dirmngr

# Repository setup and package installation handled by Ansible tasks
# This ensures tests validate actual Ansible behavior
