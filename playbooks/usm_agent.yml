---
- name: USM Agent Provisioning
  hosts: usm_agent
  gather_facts: false
  tags: usm_agent
  environment: "{{ proxy_env }}"
  tasks:
    - import_role:
        name: variables

    - import_role:
        name: usm-agent
