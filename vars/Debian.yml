openjdk_package: openjdk-8-jre
repository:
  key: "http://packages.confluent.io/deb/{{ confluent.version.major }}.{{ confluent.version.minor }}/archive.key"
  url: "deb http://packages.confluent.io/deb/{{ confluent.version.major }}.{{ confluent.version.minor }} stable main"
base_package_name: "{% if confluent.oss %}confluent-platform-oss-2.11{% else %}confluent-platform-2.11{% endif %}"
package_name: "{{ base_package_name }}{% if not confluent.latest %}={{ confluent_version | join('.')  }}{% endif %}"
