all:
  vars:
    ansible_become: true
    ansible_connection: ssh
    kerberos:
      realm: stan_test.confluent.io
kerberos_server:
  vars:
    realm_name: "{{ kerberos.realm | upper }}"
  hosts:
    ip-172-31-47-81.us-west-2.compute.internal:
      ansible_host: ec2-34-220-99-152.us-west-2.compute.amazonaws.com
      ansible_user: centos
