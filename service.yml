name: cp-ansible
lang: unknown
lang_version: unknown
git:
  enable: true
github:
  enable: true
  repo_name: confluentinc/cp-ansible
codeowners:
  enable: true
semaphore:
  enable: true
  pipeline_enable: false
  branches:
    - master
    - /[0-9]+\.[0-9]+\.x/