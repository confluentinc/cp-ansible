---
- name: restart usm agent
  systemd:
    daemon_reload: true
    name: "{{usm_agent_service_name}}"
    state: restarted
  tags:
    - systemd

- name: Startup Delay
  wait_for:
    timeout: "{{ usm_agent_health_check_delay }}"
