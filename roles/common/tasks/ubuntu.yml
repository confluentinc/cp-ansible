---
- name: Install apt-transport-https
  apt:
    name: apt-transport-https
    update_cache: true
  register: install_apt_transport_result
  until: install_apt_transport_result is success  or ansible_check_mode
  retries: 5
  delay: 90
  tags: package

- name: Install gnupg for gpg-keys
  apt:
    name: gnupg2
  register: install_gnupg_result
  until: install_gnupg_result is success  or ansible_check_mode
  retries: 5
  delay: 90
  tags: package

- name: Install python3-debian for deb822_repository module
  apt:
    name: python3-debian
  register: install_python_debian_result
  until: install_python_debian_result is success or ansible_check_mode
  retries: 5
  delay: 90
  tags: package

- name: Download Confluent Repository GPG Keys
  shell: |
    curl -fsSL "{{ item.url }}" -o "{{ item.dest }}"
    chmod 644 "{{ item.dest }}"
  args:
    creates: "{{ item.dest }}"
  loop:
    - url: "{{ confluent_common_repository_debian_clients_key_url }}"
      dest: /usr/share/keyrings/confluent-clients-archive-keyring.gpg
    - url: "{{ confluent_common_repository_debian_key_url }}"
      dest: /usr/share/keyrings/confluent-common-archive-keyring.gpg
    - url: "{{ confluent_control_center_next_gen_repository_debian_key_url }}"
      dest: /usr/share/keyrings/confluent-control-center-next-gen-archive-keyring.gpg
    - url: "{{ confluent_usm_agent_repository_debian_key_url }}"
      dest: /usr/share/keyrings/confluent-usm-agent-archive-keyring.gpg
  register: gpg_key_download_result
  until: gpg_key_download_result is success or gpg_key_download_result is skipped
  retries: 5
  delay: 90
  when:
    - repository_configuration == 'confluent'
    - installation_method == "package"
  tags: package

- name: Add Confluent Repositories using deb822_repository
  deb822_repository:
    name: "{{ item.name }}"
    types:
      - deb
    uris:
      - "{{ item.uri }}"
    suites:
      - "{{ item.suite }}"
    components:
      - main
    signed_by: "{{ item.signed_by }}"
    state: present
  loop:
    - name: confluent-clients
      uri: "{{ confluent_clients_repository_baseurl }}/clients/deb"
      suite: "{{ ansible_distribution_release }}"
      signed_by: /usr/share/keyrings/confluent-clients-archive-keyring.gpg
    - name: confluent-common
      uri: "{{ confluent_common_repository_baseurl }}/deb/{{ confluent_repo_version }}"
      suite: stable
      signed_by: /usr/share/keyrings/confluent-common-archive-keyring.gpg
    - name: confluent-control-center-next-gen
      uri: "{{ confluent_control_center_next_gen_repository_debian_baseurl }}"
      suite: stable
      signed_by: /usr/share/keyrings/confluent-control-center-next-gen-archive-keyring.gpg
    - name: confluent-usm-agent
      uri: "{{ confluent_usm_agent_repository_debian_baseurl }}"
      suite: stable
      signed_by: /usr/share/keyrings/confluent-usm-agent-archive-keyring.gpg
  when:
    - repository_configuration == 'confluent'
    - installation_method == "package"
  tags: package
  notify:
    - apt-get update


- name: Ensure custom Confluent Apt Repo file does not exist when using Confluent repositories
  file:
    state: absent
    path: /etc/apt/sources.list.d/custom_confluent.list
  when:
    - repository_configuration == 'confluent'
    - installation_method == "package"

- name: Ensure Confluent deb822 repositories are not present when repository_configuration is custom
  deb822_repository:
    name: "{{ item }}"
    state: absent
  loop:
    - confluent-clients
    - confluent-common
    - confluent-control-center-next-gen
    - confluent-usm-agent
  when:
    - repository_configuration == 'custom'

- name: Add Custom Apt Repo
  copy:
    src: "{{custom_apt_repo_filepath}}"
    dest: /etc/apt/sources.list.d/custom_confluent.list
    mode: '644'
  register: apt_custom_add_result
  until: apt_custom_add_result is success
  retries: 5
  delay: 90
  when: repository_configuration == 'custom'
  notify:
    - apt-get update

- meta: flush_handlers

- name: Make Sure man pages Directory Exists
  file:
    path: /usr/share/man/man1
    state: directory
    mode: '755'

- name: Custom Java Install
  include_tasks: custom_java_install.yml

- name: Add open JDK repo
  apt_repository:
    repo: "{{ubuntu_java_repository}}"
  #Throttle was introduced to help mitigate the APT Lock when deploying multiple components to the same node.
  throttle: 1
  register: apt_add_jdk_result
  until: apt_add_jdk_result is success
  retries: 5
  delay: 90
  when:
    - repository_configuration == 'confluent'
    - install_java|bool

- name: Install Java
  apt:
    name: "{{ubuntu_java_package_name}}"
  register: java_install_result
  until: java_install_result is success  or ansible_check_mode
  retries: 10
  delay: 5
  when: install_java|bool
  tags: package

- name: Install OpenSSL
  apt:
    name: openssl
  tags: package

- name: Get Java Version
  command: java -version
  register: version_output
  check_mode: false
  changed_when: false

- name: Print Java Version
  debug:
    msg: "Current Java Version is: {{version_output.stderr_lines[0]}}"

- name: Install pip
  apt:
    name:
      - python3-pip
    state: present
  become: true
  tags:
    - package
    - pip-package

- name: Upgrade pip
  ansible.builtin.pip:
    name: pip
    extra_args: --upgrade
  tags:
    - package
    - pip-package
  when:
    - ansible_distribution_major_version not in ['24']

- name: Install pip packages
  ansible.builtin.pip:
    name: "{{pip_packages}}"
  tags:
    - package
    - pip-package
  when:
    - ansible_distribution_major_version not in ['24']

- name: Install pip packages using apt
  apt:
    name:
      - "python3-{{item}}"
    state: present
  become: true
  loop: "{{pip_packages}}"
  tags: package
  when:
    - ansible_distribution_major_version == "24"

- name: Install pyyaml package using apt
  apt:
    name:
      - "python3-yaml"
    state: present
  become: true
  tags: package

- name: Enable FIPS on Ubuntu 22+
  include_tasks: fips-ubuntu.yml
  when:
    - ansible_distribution == "Ubuntu" and ansible_distribution_major_version in ['22', '24']
    - fips_enabled|bool
