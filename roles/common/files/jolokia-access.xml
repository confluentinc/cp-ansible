<?xml version="1.0" encoding="utf-8"?>
<restrict>

  <remote>
    <!-- Empty Block so all remote access gets blocked -->
  </remote>

  <http>
    <!-- Empty Block so all http methods get blocked -->
  </http>

  <commands>
    <!-- Empty Block so all commands get blocked -->
  </commands>

  <allow>
  </allow>

  <deny>
  </deny>

</restrict>
