---
- name: Restart Kafka
  systemd:
    daemon_reload: true
    name: "{{kafka_broker_service_name}}"
    state: restarted
  tags:
    - systemd
  when: not ansible_check_mode

- name: Startup Delay
  wait_for:
    timeout: "{{ kafka_broker_health_check_delay }}"
