# Maintained by Ansible
{# kafka_broker_properties defined in the confluent.variables role #}
{% for dict_name in ['zk_ssl', 'zk_ssl_fips', 'zk_mtls', 'zk_mtls_fips'] %}
{% if kafka_broker_properties[dict_name].enabled|bool %}
{% for key, value in kafka_broker_properties[dict_name].properties|dictsort%}
{{key}}={{value}}
{% endfor %}
{% endif %}
{% endfor %}
