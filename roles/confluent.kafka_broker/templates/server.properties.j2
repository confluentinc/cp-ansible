# Maintained by Ansible

{# kafka_broker_final_properties defined in the confluent.variables_handlers role #}
{% for key, value in kafka_broker_final_properties|dictsort%}
{{key}}={{value}}
{% endfor %}
