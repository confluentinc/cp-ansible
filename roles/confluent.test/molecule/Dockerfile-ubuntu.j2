FROM {{ item.image }}


RUN apt-get update -y 
RUN mkdir -p /usr/share/man/man1
RUN apt-get install -y \
       apt-utils \
       locales \
       gnupg \
       python3-setuptools \
       python3-pip \
       software-properties-common \
       openjdk-8-jdk \
       rsync \
       ca-certificates \
       openssl \
       unzip \
       curl \
       wget \
       rsyslog systemd systemd-cron sudo iproute2 \
    && rm -Rf /var/lib/apt/lists/* \
    && rm -Rf /usr/share/doc && rm -Rf /usr/share/man \
    && apt-get clean
RUN sed -i 's/^\($ModLoad imklog\)/#\1/' /etc/rsyslog.conf



# # TODO go w official repo link
# # Deb Packages: https://s3-us-west-2.amazonaws.com/staging-confluent-packages-5.5.0/deb/5.5

RUN wget -qO - https://s3-us-west-2.amazonaws.com/staging-confluent-packages-5.5.0/deb/5.5/archive.key | apt-key add -
RUN add-apt-repository "deb [arch=amd64] https://s3-us-west-2.amazonaws.com/staging-confluent-packages-5.5.0/deb/5.5 stable main"
RUN apt-get update -y

RUN apt-get install -y \
      confluent-common \
      confluent-rebalancer \
      confluent-rest-utils \
      confluent-metadata-service  \
      confluent-server \
      confluent-kafka-connect-elasticsearch \
      confluent-kafka-connect-jdbc \
      confluent-kafka-connect-jms \
      confluent-kafka-connect-replicator \
      confluent-kafka-connect-s3 \
      confluent-kafka-connect-storage-common \
      confluent-kafka-rest \
      confluent-ksqldb \
      confluent-schema-registry \
      confluent-security \
      confluent-control-center-fe \
      confluent-control-center \
      confluent-schema-registry
